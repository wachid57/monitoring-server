# ==============================================
# FRONTEND CONFIGURATION
# ==============================================
VITE_BACKEND_URL=http://localhost:7080
VITE_API_PREFIX=/api/v1.0
VITE_SITE_TITLE=WCLOUD.my.id
VITE_SITE_DESCRIPTION=WCLOUD.my.id - Dashboard Monitoring Website

# ==============================================
# DATABASE CONFIGURATION (MariaDB/MySQL)
# ==============================================
DB_HOST=nac-server-mariadb-1
DB_PORT=3306
DB_USER=romyuser
DB_PASS=mypass
DB_NAME=nac-db

# ==============================================
# SECURITY CONFIGURATION
# ==============================================
# JWT Secret Key - CHANGE THIS IN PRODUCTION!
JWT_SECRET=change-this-to-a-secure-256-bit-random-key-in-production

# ==============================================
# REDIS CONFIGURATION (Session & Cache)
# ==============================================
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
# Uncomment and set password for production:
# REDIS_PASSWORD=your-strong-redis-password

# ==============================================
# APPLICATION CONFIGURATION
# ==============================================
# Environment (development, staging, production)
APP_ENV=development
# Application port (if different from default)
APP_PORT=8080
# Log level (debug, info, warn, error)
LOG_LEVEL=debug

# ==============================================
# MONITORING & METRICS
# ==============================================
# Enable metrics collection
ENABLE_METRICS=true
# Metrics port
METRICS_PORT=9090

# ==============================================
# CORS CONFIGURATION
# ==============================================
# Enable CORS middleware (true/false). Default false for safety.
ENABLE_CORS=false
# Alternative toggle: set CORS_ENABLE=true (or 'all' to allow all origins)
CORS_ENABLE=
# Comma-separated allowed origins or hostnames/IPs.
# Examples:
#   CORS_DOMAIN=localhost,192.168.21.167
#   CORS_DOMAIN=http://localhost:5173,https://app.example.com
# If hostnames/IPs (no scheme) are provided, common dev variants will be allowed automatically.
CORS_DOMAIN=

# ==============================================
# PRODUCTION SECURITY NOTES
# ==============================================
# 1. Generate strong JWT_SECRET: openssl rand -hex 32
# 2. Set strong REDIS_PASSWORD
# 3. Use secure DB_PASS
# 4. Set APP_ENV=production
# 5. Set LOG_LEVEL=info or warn
